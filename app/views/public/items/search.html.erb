<h3>検索結果</h3>

<% if @items.present? %>
  <ul>
    <% @items.each do |item| %>
      <li>
        <h3><%= item.name %></h3><br>
        <h3><%= number_to_currency(item.price, unit: "円") %></h3><br>
        <%= link_to "詳細を見る", public_item_path(item), class: "btn btn-sm btn-primary" %>
      </li>
      <% if item.images.attached? %>
       <% item.images.each do |image| %>
       <%= image_tag image.variant(resize_to_limit: [300,300]), class: "item-image" %>
      <% end %>
      <% else %>
       <p>画像は未登録です</p>
      <% end %>
    <% end %>
  </ul>
<% else %>
  <p>該当する商品は見つかりませんでした。</p>
<% end %>
