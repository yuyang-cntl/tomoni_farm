<div class="vh-100">
<h3>フォロー中の農家一覧</h3>

 <% if current_customer.followed_farmers.any? %>
  <% current_customer.followed_farmers.each do |farmer| %>
   <div class="farmer-card">
   <h3><%= farmer.name %></h3>
   <h3><%= farmer.introduction %></h3>
    <% if farmer.image.attached? %>
     <%= image_tag farmer.image.variant(resize_to_limit: [300,300]), class: "farmer-image" %>
    <% else %>
     <%= image_tag(farmer.get_profile_image(100, 100), alt: "Farmer image", class: "profile-image") %>
    <% end %>
    <%= link_to '日記一覧', public_farmer_diaries_path(farmer), style:"font-size: 24px; font-weight: bold;" %>
   </div>
  <% end %>
 <% else %>
  <h3>フォロー中の農家はまだありません。</h3>
 <% end %>
</div>